server:
  port: 8082

spring:
  application:
    name: complaint-service   # 🔥 VERY IMPORTANT

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

  instance:
    prefer-ip-address: true

jwt:
  secret: resolvelt-super-secret-key-which-should-be-at-least-32-bytes
  expiration: 864000
